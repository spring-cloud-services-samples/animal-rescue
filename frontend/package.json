{
  "name": "untitled",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.2",
    "prop-types": "^15.7.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-responsive-carousel": "^3.1.51",
    "react-scripts": "3.3.1",
    "semantic-ui-react": "^0.88.2"
  },
  "scripts": {
    "start": "wait-on tcp:8080 && REACT_APP_LOGIN_PATH=http://localhost:8080/oauth2/authorization/sso react-scripts start",
    "build": "REACT_APP_BACKEND_BASE_URL=/api react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:8080",
  "homepage": "/rescue",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "wait-on": "^4.0.0"
  }
}
